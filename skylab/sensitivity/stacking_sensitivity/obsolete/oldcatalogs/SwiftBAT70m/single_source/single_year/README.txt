The difference between this and stacked_sources is that this directory implements the mc energy range of diffuse flux. The background trials and script framework should be identical, but the injection range is implemented in calculations, and the results are spit into a separate folder.

